/**
 * Element Plus persistence boundary.
 * Sprint 00 establishes this boundary; product behavior follows in later sprints.
 */
export const persistenceBoundary = "@element-plus/persistence" as const;
