import { buildDraft, transitionSps, type StructuredLlm } from "@element-plus/founder";
export type FounderStore={createProblem(input:{workspaceId:string;actorId:string;rawStatement:string}):Promise<{problemId:string;sessionId:string}>;appendMessage(input:{sessionId:string;content:string}):Promise<void>;saveDraft(input:{problemId:string;draft:unknown}):Promise<void>;confirm(input:{sessionId:string;problemId:string;actorId:string}):Promise<void>};
export class FounderService { constructor(private readonly store:FounderStore,private readonly llm:StructuredLlm){} async start(input:{workspaceId:string;actorId:string;rawStatement:string}){const result=await this.store.createProblem(input);await this.store.appendMessage({sessionId:result.sessionId,content:input.rawStatement});return result;} async structure(input:{problemId:string;rawStatement:string}){const draft=await buildDraft(input.rawStatement,this.llm);await this.store.saveDraft({problemId:input.problemId,draft});return draft;} async confirm(input:{sessionId:string;problemId:string;actorId:string}){transitionSps("validating","completed",true);await this.store.confirm(input);}}
