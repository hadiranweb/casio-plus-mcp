id: primitive.workflow
version: 0.1.0
fields:
  - { name: id, type: string, required: true }
  - { name: title, type: string, required: true }
  - { name: owner, type: string, required: false }
  - { name: status, type: enum, required: true, allowed_values: [draft, evidence_collected, active, automated] }
  - { name: asset_status, type: enum, required: true, allowed_values: [draft, evidence_collected, active] }
  - { name: stages, type: array, required: false }
  - { name: data_return_paths, type: array, required: false }
lifecycle: [draft, evidence_collected, active, automated]
validity_rules:
  []
