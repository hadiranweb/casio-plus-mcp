import { AudioLines, FileText, Image, Newspaper, Send } from 'lucide-react';
import { PageHeader } from '@/components/PageHeader';
import { Badge, Dot, Label, SectionHead } from '@/components/terminal';
import { casioContentModel } from '@/lib/casio-content';
import { num, t } from '@/lib/i18n';
export const dynamic = 'force-dynamic';
const ICONS=[Send,FileText,FileText,Send,Image,Newspaper];
export default function CasioContentPage(){const model=casioContentModel();return <div dir="rtl"><PageHeader eyebrow={t('pages.content.eyebrow')} title={t('pages.content.title')} right={<Badge tone="accent">v1.1</Badge>}/><div className="-mt-3 mb-[18px] font-mono text-[12px] text-os-muted"><span className="text-os-accent">{t('content.oneTopic')}</span> <span className="text-os-border-strong">·</span> رشتهٔ محتوایی <span className="text-os-border-strong">·</span> بازگشت داده به حافظه</div><section className="mb-5 grid gap-4 xl:grid-cols-[1.45fr_.55fr]"><div className="rounded-lg-t border border-os-border bg-os-surface p-5"><SectionHead label={t('content.formats')} count={t('content.formatsCount', { count: num(model.formats.length) })}/><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{model.formats.map(([name,description],i)=>{const I=ICONS[i];return <div key={name} className="hoverable rounded-md-t border border-os-border bg-os-surface2 p-4"><I className="h-4 w-4 text-os-accent"/><h2 className="mt-4 text-sm font-bold">{name}</h2><p className="mt-2 font-mono text-[10px] leading-relaxed text-os-dim">{description}</p></div>})}</div></div><div className="rounded-lg-t border border-os-border bg-os-surface p-5"><Label>{t('content.dailyRule')}</Label><h2 className="mt-3 text-lg font-bold">یک موضوع.<br/>یک مسیر ذهنی.</h2><p className="mt-3 font-mono text-[10px] leading-relaxed text-os-dim">هر روز یک سوژه اصلی؛ ۸ تا ۱۰ پست حداکثر؛ شروع هر رشته با بنر موضوعی.</p><div className="mt-5 border-t border-os-border pt-4"><Dot state="available"/> <span className="mr-2 font-mono text-[10px] text-os-muted">{t('content.awaitAdapter')}</span></div></div></section><section className="rounded-lg-t border border-os-border bg-os-surface p-5"><SectionHead label={t('content.assets')} count={t('content.assetsCount', { count: num(model.assets.length) })}/><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-4">{model.assets.map((asset:any)=><div key={asset.id} className="rounded-md-t border border-os-border bg-os-surface2 p-4"><Badge tone={asset.برچسب_توسعه==='توسعه'?'warn':'ok'}>{asset.برچسب_توسعه==='توسعه'?'توسعه':'داریم'}</Badge><h3 className="mt-4 text-[12px]">{asset.نام_پلی_بوک}</h3><p className="mt-2 font-mono text-[9px] leading-relaxed text-os-dim">{asset.مثال_اجرایی}</p></div>)}</div><div className="mt-5 flex items-center gap-2 border-t border-os-border pt-4 font-mono text-[10px] text-os-dim"><AudioLines className="h-4 w-4 text-os-accent"/> وویس جلسات آموزشی به واحدهای زیر ۵ دقیقه تقطیع می‌شوند؛ هر وویس یک دارایی محتوایی مستقل است.</div></section></div>}
