# Knowledge — کاسیو پلاس
# منبع حقیقت دانش برای workspace کاسیو

این پوشه حافظه سازمانی کاسیو است:
- `casio.yaml` — مدل دانش HEGAM + ۵۶ پلی‌بوک (نسخه 0.4.1) — منبع حقیقت فعلی
- `knowledge.yaml` — alias برای سازگاری با مسیر استاندارد workspace

 lineage:
   - از `knowledge/casio.yaml` در ریشه ریپو مهاجرت شده
   - هر تغییر از مسیر Proposal → Review → Merge عبور می‌کند
   - نسخه‌بندی semver — `knowledgeSummary` یکپارچگی را چک می‌کند
